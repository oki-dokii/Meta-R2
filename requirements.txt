# LifeStack — Requirements
# Python 3.9+

# Core LLM client (Groq uses OpenAI-compatible API)
openai>=1.30.0
pydantic>=2.7.0

# OpenEnv — mandatory hackathon requirement
openenv-core>=0.2.3

# OpenEnv runtime dependencies (must be explicit to survive Docker + HF Spaces)
fastapi>=0.110.0
fastmcp>=2.0.0
httpx>=0.27.0

# RAG Memory
chromadb>=0.5.0
sentence-transformers>=2.7.0

# Flask demo UI (Replacing Gradio for compatibility)
flask>=3.0.0

# Plotting
matplotlib>=3.8.0
numpy>=1.26.0
gymnasium>=0.29.0
uvicorn>=0.30.0

# Utilities (stdlib — listed for clarity)
# json, os, copy, random, math, uuid, shutil, datetime — no install needed

# ── Training framework (GPU instance required for full training) ──────────────
torch>=2.1.0
transformers>=4.40.0
# TRL >= 1.1 for GRPOTrainer with full feature set
trl>=1.1.0
accelerate>=0.29.0
peft>=0.10.0
bitsandbytes>=0.43.0
datasets>=2.14.0

# Unsloth — fast fine-tuning (hackathon judging requirement)
# Standard install works for most GPU environments (A100, H100, T4, L4):
#   pip install unsloth
# For HF Spaces / Colab with known CUDA version:
#   pip install "unsloth[cu121-torch240]"   # CUDA 12.1 + PyTorch 2.4.0
#   pip install "unsloth[cu118-torch220]"   # CUDA 11.8 + PyTorch 2.2.0
# Fallback (always works, slightly slower first run):
#   pip install "unsloth @ git+https://github.com/unslothai/unsloth.git"
unsloth

# Google API (Gmail Intake)
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.128.0

# HuggingFace Hub (F3 InferenceClient, F8 push_to_hub)
huggingface_hub>=0.23.0

# Calendar upload parsing (F10)
icalendar>=5.0.0
